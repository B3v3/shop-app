<div class="container">
  <% if current_order.ordered_products.any? %>
    <%= render 'orders/not_empty_cart' %>
  <% else %>
    <div class="jumbotron text-center">
      <h1> Empty Cart ): </h1>
    </div>
  <% end %>
</div>

  <div class="jumbotron text-center">
    <% @tags.each_slice(3) do |three_tags| %>
        <ul class="list-inline">
        <% three_tags.each do |tag| %>
          <li class="list-inline-item">
            <%= link_to "#{tag.name}\t", tag, class: 'h3 text-dark' %>
          </li>
        <% end %>
        </ul>
    <% end %>
  </div>

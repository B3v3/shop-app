<%= render 'tags_menu' if @tags.any? %>

<%= link_to "Create a new product", new_product_path,
             class: 'btn btn-info btn-lg btn-block' if
             current_user && current_user.is_admin? %>

<% if @products.any? %>
    <%= render "products" %>
<% else %>
  <%= render 'none_products' %>
<% end %>
